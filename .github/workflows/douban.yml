
name: douban
on: 
  schedule:
  - cron: "30 * * * *"

jobs:
  douban:
    name: Douban mark data sync
    runs-on: ubuntu-latest
    steps:
    - name: movie
      uses: xia0bs/doumark-action@master
      with:
        id: "151651419"
        type: movie
        format: notion
        dir: "10e245c4d67c478d9dc928b5659c136a"
        notion_token: ${{ secrets.notion_token }}
